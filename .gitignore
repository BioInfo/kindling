# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# data — NEVER commit financial data
*.db
*.db-shm
*.db-wal
/data/

# secrets — keys live in `pass`, never in files
.env
.env.*
!.env.example

# personal deployment overlay — never ships
local.env
CLAUDE.local.md
ROADMAP.md
docs/NEXT-SESSION.md
.learnings/
scripts/seed-*-holdings.mjs
!scripts/seed-manual-holdings.example.mjs

# os / editor
.DS_Store
*.log

# screenshots / scratch — may contain real financial data
*.png
# …but brand assets are safe and must ship with the repo
!app/icon.png
!app/apple-icon.png
!public/logo.png
.playwright-mcp/

# launchagent logs
kindling.log
kindling.err.log

# claude code runtime locks (never track)
.claude/scheduled_tasks.lock

# TS incremental compile cache (regenerated by tsc --noEmit)
tsconfig.tsbuildinfo
*.tsbuildinfo
